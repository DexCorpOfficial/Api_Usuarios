@model Api_Post.Models.Cuenta

@{
    ViewData["Title"] = "Delete";
}

<h1>Eliminar Cuenta</h1>

<h3>¿Está seguro de que desea eliminar esta cuenta?</h3>
<hr />
<div>
    <h4>Detalles de la cuenta</h4>
    <dl class="row">
        <dt class="col-sm-2">Nombre</dt>
        <dd class="col-sm-10">@Model.Nombre</dd>

        <dt class="col-sm-2">Foto de Perfil</dt>
        <dd class="col-sm-10">
            @if (Model.FotoPerfil != null)
            {
                <img src="data:image;base64,@(Convert.ToBase64String(Model.FotoPerfil))" alt="Foto de Perfil" style="max-width: 150px; max-height: 150px;" />
            }
            else
            {
                <p>No hay foto de perfil</p>
            }
        </dd>

        <dt class="col-sm-2">Biografía</dt>
        <dd class="col-sm-10">@Model.Biografia</dd>

        <dt class="col-sm-2">Seguidores</dt>
        <dd class="col-sm-10">@Model.Seguidores</dd>

        <dt class="col-sm-2">Género</dt>
        <dd class="col-sm-10">@Model.Genero</dd>

        <dt class="col-sm-2">Seguidos</dt>
        <dd class="col-sm-10">@Model.Seguidos</dd>

        <dt class="col-sm-2">Fecha de Creación</dt>
        <dd class="col-sm-10">@Model.FechaCreacion</dd>

        <dt class="col-sm-2">Fecha de Nacimiento</dt>
        <dd class="col-sm-10">@Model.FechaNacimiento</dd>

        <dt class="col-sm-2">Es Músico</dt>
        <dd class="col-sm-10">@Model.Musico</dd>

        <dt class="col-sm-2">Activo</dt>
        <dd class="col-sm-10">@Model.Activo</dd>

        <dt class="col-sm-2">Privado</dt>
        <dd class="col-sm-10">@Model.Privado</dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Eliminar" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </form>
</div>
